<?xml version="1.0"?>
<psalm
    errorLevel="3"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
    findUnusedBaselineEntry="true"
    findUnusedCode="false"
>
    <projectFiles>
        <directory name="src"/>
        <ignoreFiles>
            <directory name="vendor"/>
        </ignoreFiles>
    </projectFiles>
    <issueHandlers>
        <MissingOverrideAttribute errorLevel="suppress"/>
        <!-- Optional dependencies (Telescope, Horizon, Reverb) are checked dynamically -->
        <MissingDependency errorLevel="suppress"/>
        <UndefinedClass errorLevel="suppress"/>
        <!-- Laravel interface parameter naming inconsistencies -->
        <ParamNameMismatch errorLevel="suppress"/>
        <!-- Laravel magic methods and dynamic calls -->
        <UndefinedMagicMethod errorLevel="suppress"/>
        <!-- Laravel interface return type mismatches (bool vs int, etc) -->
        <ImplementedReturnTypeMismatch errorLevel="suppress"/>
        <!-- Laravel parent class methods not always visible to static analysis -->
        <UndefinedMethod errorLevel="suppress"/>
        <!-- Laravel factory template system false positive -->
        <InvalidTemplateParam errorLevel="suppress"/>
    </issueHandlers>
    <plugins>
        <pluginClass class="Psalm\LaravelPlugin\Plugin"/>
    </plugins>
</psalm>
